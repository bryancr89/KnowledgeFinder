'use strict';

describe('Routes', function () {
	it('should define the route for GET categories/', function () {

	});

	it('should define the route for GET categories/id', function () {

	});

	it('should return an error if the category doesn\'t exist GET categories/id', function () {

	});

	it('should define the route for GET a range of categories/?start', function () {

	});

	it('should define the route for POST categories/', function () {

	});

	it('should define the route for PUT categories/', function () {

	});

	it('should define the route for DELETE categories/', function () {

	});
});